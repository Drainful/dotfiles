(use-modules (gnu)
             (gnu packages web-browsers)
             (gnu packages lisp)
             (my-packages)
             (lib))

;; next installed from git repo

(os-part (list)
         (list sbcl)
         (list))
